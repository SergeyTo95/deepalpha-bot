name: Apply Kimi stream UTF-8 fix

on:
  push:
    branches:
      - fix/velia-kimi-stream-utf8
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: fix/velia-kimi-stream-utf8
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install focused test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest

      - name: Apply focused product patch
        run: python tools/apply_kimi_stream_utf8_fix.py

      - name: Compile changed gateway
        run: python -m py_compile services/kimi_streaming_gateway.py

      - name: Run focused streaming regressions
        run: |
          pytest -q \
            tests/test_kimi_streaming_gateway.py \
            tests/test_velia_mobile_streaming_service.py

      - name: Commit verified product changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm tools/apply_kimi_stream_utf8_fix.py
          git rm .github/workflows/apply-kimi-stream-utf8-fix.yml
          git add services/kimi_streaming_gateway.py tests/test_kimi_streaming_gateway.py
          git commit -m 'Fix Kimi streaming UTF-8 decoding'
          git push origin HEAD:fix/velia-kimi-stream-utf8
