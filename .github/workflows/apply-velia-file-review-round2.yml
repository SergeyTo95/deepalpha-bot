name: Apply VELIA File Analyst review round 2

on:
  push:
    branches:
      - feature/velia-file-analyst-attachments
    paths:
      - .github/workflows/apply-velia-file-review-round2.yml
      - .github/velia-file-review-round2.patch

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/velia-file-analyst-attachments
          fetch-depth: 0

      - name: Apply review fixes
        run: |
          git apply --recount --check .github/velia-file-review-round2.patch
          git apply --recount .github/velia-file-review-round2.patch
          rm -f .github/velia-file-review-round2.patch
          rm -f .github/workflows/apply-velia-file-review-round2.yml

      - name: Commit review fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: preserve File Analyst transport semantics"
          git push origin HEAD:feature/velia-file-analyst-attachments
